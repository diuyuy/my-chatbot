# AppSidebarHistoryMenu 수정 작업

서버 컴포넌트인 `AppSidebarHistoryMenu`을 클라이언트 컴포넌트로 리팩토링 하고, `.claude_contexts\frontend\tasks\use-query.md` 파일 내용 참고해서 `useQuery`를 사용한 `conversations` 상태 관리 기능 구현해주세요.

`src\client-services\conversation.api.ts` 파일에 있는 `fetchHistoryMessages()` 함수 사용해서 데이터를 가져와주세요.

