# handleDelete 구현 작업

`src\app\(main)\workspace\resource\[resourceId]\components\embeddings-list.tsx`에서 `handleDelete` 서버에 요청하는 로직 다음 요구사항을 참고해서 구현해주세요.

**요구사항**:
1. `.claude_contexts\frontend\tasks\use-mutation.md` 내용에 가이드대로 useMutation 사용해서 구현해주세요.
2. `src\client-services\rag.api.ts`의 `deleteResourceById()`와 `deleteChunkById()` 사용해서 삭제 기능 구현해주세요.